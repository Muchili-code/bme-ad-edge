# Foundation 01：创建工程骨架

## 所属模块

`coordination/modules/00_foundation_module.md`

## 目标

创建 `ad-edge-demo/` 基础目录、`.env.example` 和 README。

## 执行前必须阅读

1. `AGENT.md`
2. `detail plan.md`
3. `coordination/PROCESS_RULES.md`
4. 所属模块文件
5. 与本任务相关的契约文件

## 写入范围

按所属模块文件规定执行，不越界修改。

## 完成后

1. 运行与本任务相关的最小自测。
2. 更新对应 `coordination/status/foundation.status.md`。
3. 在状态文件列出修改文件、自测结果、阻塞问题和需要母进程检查的点。
