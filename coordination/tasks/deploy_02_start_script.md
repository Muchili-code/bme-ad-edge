# Deploy 02：一键启动脚本

## 所属模块

`coordination/modules/04_deploy_module.md`

## 目标

实现 `start_demo.sh` 和环境检查。

## 执行前必须阅读

1. `AGENT.md`
2. `detail plan.md`
3. `coordination/PROCESS_RULES.md`

## 写入范围

`start_demo.sh`、`.env.example`、`docs/`。

## 完成后

1. WSL 中可运行启动脚本。
2. Windows shell 直接运行时给出改用 WSL 的提示。
3. 更新 `coordination/status/deploy.status.md`。
