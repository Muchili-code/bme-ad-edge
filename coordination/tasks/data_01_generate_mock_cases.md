# Data 01：生成 mock 病例包

## 所属模块

`coordination/modules/01_data_module.md`

## 目标

实现 `generate_mock_cases.py`，生成 HC/MCI/AD 三个病例。

## 执行前必须阅读

1. `AGENT.md`
2. `detail plan.md`
3. `coordination/PROCESS_RULES.md`
4. `coordination/CASE_PACKAGE_SPEC.md`
5. `coordination/OUTPUT_FILE_SPEC.md`

## 写入范围

`scripts/` 与 `data/cases/`。

## 完成后

1. 运行生成脚本。
2. 更新 `coordination/status/data.status.md`。
3. 在状态文件列出修改文件、自测结果、阻塞问题和需要母进程检查的点。
